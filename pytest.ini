[pytest]
# Optional: for running pytest locally. CI uses Django's runner: python manage.py test
# Exclude _extra so standalone scripts there are not collected as tests
norecursedirs = .git .venv _extra node_modules __pycache__ *.egg-info staticfiles media
