"""Canonical expected versions for manually deployed SQL Server objects.

Bump the matching value here when the repo SQL artifact's PROC_VERSION changes.
"""

PROC_VERSIONS = {
    "dbo.usp_process_award_staging": "2026-07-24.1",
}
